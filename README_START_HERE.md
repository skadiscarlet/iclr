# R02C 任务包

此包承接GitHub提交148f59948200bfe5d25a30c8c5115f613dc4f9fb，继续research/naacl2027-r02。

1. 按相对路径合并，不盲目覆盖未提交文件。
2. 本地执行：

```text
/goal 执行 plans/rounds/R02C.md。先读 plans/EXECUTION_CONTRACT.md。承接148f59948200bfe5d25a30c8c5115f613dc4f9fb，继续research/naacl2027-r02。先离线诊断全部旧输出和历史请求，再集成严格输出契约、真实SBS卡片更新与E0前置检查；补齐两张逻辑要求卡。通过前置条件后只跑一对有界本地冻结先导，新增最多48次预约，不重新训练或更换模型。更新任务总结、本地资产与运行记录，实际提交推送并核对远程SHA，只执行R02C。
```

提供了实际可用的纯Python输出parser和43项自建测试。它们只验证包装与结构，不检测代码漏洞、不生成模型输出、不评定语义。集成runner和真实执行仍由本地agent完成，不能把包内工具测试说成全系统通过。

schemas是结构文档；输出契约的严格额外约束（重复键、非有限JSON数值、只允许整条围栏、空白字符串）由parser执行。发布/归档不得含原始第三方源码、秘密或模型权重。完整数据和输出留本地，报告给必要可核验内容。
